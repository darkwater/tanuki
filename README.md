Tanuki
======

(early prototyping)

- Home Assistant alternative
- WASM plugin-based architecture
- MQTT core with HTTP for additional functionality (config, video streams, ...)
- Capability-based entities (this "lamp" has capabilities on-off and light)
